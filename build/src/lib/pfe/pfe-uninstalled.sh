# generated by configure / remove this line to disable regeneration
prefix="/usr/local"
exec_prefix="${prefix}"
bindir="${exec_prefix}/bin"
libdir="/home/guidod/own/workspace/pfe-34/build/src/lib/pfe/.libs"
datadir="${datarootdir}"
sysconfdir="${prefix}/etc"
includedir="/home/guidod/own/workspace/pfe-34/build/src/lib/pfe/../../../../src/lib/pfe"
package="pfe"
suffix="-forth"

for option; do case "$option" in --list-all|--name) echo  "pfe"
;; --help) pkg-config --help ; echo Buildscript Of "Portable Forth Environment"
;; --modversion|--version) echo "0.34.01"
;; --requires) echo : ""
;; --libs) echo -L${libdir} "-W,--warn-common" "-lpfe -ldl"
       :
;; --cflags) echo -I${includedir} ""
       :
;; --variable=*) eval echo '$'`echo $option | sed -e 's/.*=//'`
;; --uninstalled) exit 0 
;; *) ;; esac done
